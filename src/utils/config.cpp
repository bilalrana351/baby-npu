#include "../../include/utils/config.hpp"

// Config parsing is header-only, but this file exists for future extensions

namespace npu {

// Future: Add config file parsing, environment variable support, etc.

} // namespace npu
